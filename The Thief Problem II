
class Solution {
    public static int getMaxVal(int arr[], int k) {

        
        Arrays.sort(arr);
        
        int sum  = 0;
        int n = arr.length;
        
        for(int i = n-1; i >= n - k; i--){
            sum += arr[i];
        }
        return sum;
    }
}
